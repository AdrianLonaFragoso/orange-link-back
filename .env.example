# Base de datos PostgreSQL (requerido en produccion)
# Configurar en Vercel: Settings > Environment Variables
DATABASE_URL=postgresql://usuario:password@host:5432/orangelink

# Puerto del servidor local (opcional, por defecto 3001)
PORT=3001

# ID de usuario por defecto para desarrollo (opcional)
# DEFAULT_USER_ID=uuid-del-usuario
